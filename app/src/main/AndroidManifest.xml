<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab6">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".task1.SingletonApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab6">
<!--        <activity-->
<!--            android:name=".task4.LibraryActivity"-->
<!--            android:exported="true" />-->
        <!-- <activity -->
        <!-- android:name=".task3.CoroutinesActivity" -->
        <!-- android:exported="true" /> -->
        <!-- <activity -->
        <!-- android:name=".task2.ExecutorServiceActivity" -->
        <!-- android:exported="true" /> -->
        <!-- <activity -->
        <!-- android:name=".task1.CoroutinesApplication" -->
        <!-- android:exported="true" /> -->
        <!-- <activity -->
        <!-- android:name=".ExecutorServiceActivity" -->
        <!-- android:exported="true" -->
        <!-- android:label="@string/title_activity_executor_service" -->
        <!-- android:theme="@style/Theme.Lab6.NoActionBar" /> -->
        <activity
            android:name=".task4.LibraryActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>